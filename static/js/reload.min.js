// //function b(a){var c=new WebSocket(a);c.onclose=function(){setTimeout(function(){b(a)},2E3)};c.onmessage=function(){location.reload()}}try{if(window.WebSocket)try{b("ws://localhost:12450/reload")}catch(a){console.error(a)}else console.log("Your browser does not support WebSockets.")}catch(a){console.error("Exception during connecting to Reload:",a)};


// (function () {
//     /**
//      * Establishes a WebSocket connection for auto-reload functionality.
//      * If the connection closes, it retries after a 2-second delay.
//      * @param {string} url - WebSocket URL
//      */
//     function connectWebSocket(url) {
//         var socket = new WebSocket(url);

//         // On connection close, retry the connection
//         socket.onclose = function () {
//             setTimeout(function () {
//                 connectWebSocket(url);
//             }, 2000); // Retry after 2 seconds
//         };

//         // On receiving a message, reload the page
//         socket.onmessage = function () {
//             location.reload();
//         };

//         // Error logging
//         socket.onerror = function (error) {
//             console.error("WebSocket Error:", error);
//         };
//     }

//     // Check if the browser supports WebSocket
//     if ("WebSocket" in window) {
//         try {
//             connectWebSocket("ws://localhost:12450/reload");
//         } catch (e) {
//             console.error("Failed to connect to WebSocket:", e);
//         }
//     } else {
//         console.warn("Your browser does not support WebSockets.");
//     }
// })();
